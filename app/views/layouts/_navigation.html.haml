= link_to root_path, :class => 'brand' do
  = image_tag 'logo.png'

%ul.nav#project_control
  %li#project_new
    = link_to '#/new' do
      %i.icon-folder
      &nbsp;
      New project

%ul.nav#scale-nav
  -#%li
    -#%p.navbar-text
      -#%i.icon-tasks
  %li.divider-vertical
  -#%li
    -#.navbar-text
      -#.btn-group
        -#.btn.btn-inverse.btn-mini week
        -#.btn.btn-inverse.btn-mini month
        -#.btn.btn-inverse.btn-mini year
  %li#scale-days.active= link_to 'Week', '#days'
  %li#scale-weeks= link_to 'Month', '#weeks'
  %li#scale-months= link_to 'Year', '#months'


%ul.nav.pull-right{:style=>'font-size: 16px'}
  - if !current_user or current_user.incognito?
    %li.signup
      .navbar-text
        Sign up to save your projects &rarr;

    = render :partial => 'auth/facebook'
    = render :partial => 'auth/twitter'
    = render :partial => 'auth/google'

  - else
    %li
      = link_to 'javascript: void()' do
        %i.icon-user
        &nbsp;
        = current_user
    = render :partial => 'auth/facebook'
    = render :partial => 'auth/twitter'
    = render :partial => 'auth/google'
    %li.divider-vertical
    %li.signout
      = link_to logout_path do
        %i.icon-logout

  -#- if user_signed_in?
    -#%li
      -#= link_to 'Logout', destroy_user_session_path, :method=>'delete'
  -#- else
    -#%li
      -#= link_to 'Login', new_user_session_path
  -#- if user_signed_in?
    -#%li
      -#= link_to 'Edit account', edit_user_registration_path
    -#- if current_user.has_role? :admin
      -#%li
        -#= link_to 'Admin', users_path
  -#- else
    -#%li
      -#= link_to 'Sign up', new_user_registration_path
